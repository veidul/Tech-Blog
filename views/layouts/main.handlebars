<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    {{!-- add links to css here --}}
    <link>
    <link>
</head>
<body>
    <div>
    <header>
    <h1>Tech Blog!</h1>
    <nav>
        {{#if logged_in}}
        <a href="/home">Home</a> |
        <a href="/dashboard">Dashboard</a> |
        <button class="no-button" id="logout">logout</button>
        {{else}}
        <a href="/login">Login</a>
        {{/if}}
        </nav>
    </header>
    <main>
        {{!-- renders sublayout --}}
        {{{body}}}
    </main>
    <footer>

    </footer></div>

    {{#if logged_in}}
    {{!-- add script link here for when the users that are logged in only --}}
    <script></script>
    {{/if}}
</body>
</html>